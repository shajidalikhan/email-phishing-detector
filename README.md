# Phishing Email Detection Framework

A comprehensive, dual-architecture machine learning framework designed to analyze, classify, and extract suspicious patterns from email content. 

This project goes beyond simple binary classification by providing a full suite of **Threat Indicators**, including Link/Attachment enumeration, rule-based keyword flagging, and mathematical model explainability to show *why* an email is considered dangerous.

---

## 1. Project Overview

To ensure robustness, speed, and accuracy, this project implements two separate modeling approaches that run side-by-side:

1. **Deep Learning Model (BERT):** A HuggingFace `BertForSequenceClassification` model fine-tuned on ~175,000 emails. It achieves state-of-the-art accuracy (97.9%) by understanding the deep, bidirectional context of the email text.
2. **Classical ML Model (TF-IDF + Logistic Regression):** A highly optimized, lightweight Scikit-Learn pipeline. While slightly less accurate than BERT (~96.3%), it runs instantaneously on standard CPUs and provides complete mathematical transparency (Explainability) into its decision-making process.

### Data Sources
The models were trained and evaluated on a combined dataset derived from real-world sources:
- [PhishTank](https://www.phishtank.com/) (For active phishing lures)
- The Enron Email Dataset (For benign corporate communication)

---

## 2. Documentation & Results

We highly encourage reading the deep-dive reports generated by the team to understand the architecture, the results, and the edge-cases where the models fail.

- 📊 **[Executive Summary & Results](reports/results.md):** The definitive comparison of the two models, including metrics and a breakdown of real-world edge cases.
- 🧠 **[Classical ML Report](docs/Phishing_Detection_Report.md):** A walkthrough of building, tuning, and selecting the Logistic Regression baseline.
- 🤖 **[BERT Evaluation Report](docs/bert_evaluation_report.md):** The raw metrics and Confusion Matrix for the Deep Learning model.
- ⚠️ **[BERT Failure Analysis](docs/bert-failure.md):** Critical insights into the model's vulnerabilities regarding Spear-Phishing and Automated Alerts.
- 📁 **[Directory Structure](docs/DIRECTORY_STRUCTURE.md):** Understand the layout of the repository.
- 🤝 **[Contributing & Team Roles](docs/TEAM_ROLES.md):** Guidelines on how to clone, setup, and work with git branches.

---

## 3. Getting the Data & Models

Because the trained BERT model (~438 MB) and the full dataset are too large to host on GitHub, you must obtain them locally before running the application.

**Download from Google Drive (Recommended)**
1. Download the `bert_model_export.zip` and `Phishing_Email.csv` files from the team's shared Google Drive folder: [Project Data & Models (Google Drive)](https://drive.google.com/drive/folders/1yknt9TJawHzMeJrKEk2_lU_CbjakdrQY?usp=sharing)
2. Extract the BERT model zip file directly into the `models/bert/` directory.
3. Place the `Phishing_Email.csv` dataset directly into the `data/external/` directory.

---

## 4. How to Use the Project

We provide three distinct ways to interact with the models depending on your needs.

### Method A: The Web Dashboard (Streamlit)
The easiest and most powerful way to test the models is through our interactive Web Dashboard. It runs both models concurrently and extracts visual Threat Indicators.

1. Ensure your virtual environment is activated and dependencies are installed.
2. Run the Streamlit app:
   ```bash
   streamlit run app/app.py
   ```
3. Open your browser to `http://localhost:8501`.
4. You can either **Paste Text** directly or **Upload an .eml File** to automatically parse attachments and body text!

### Method B: Interactive CLI Tester
If you prefer the terminal, you can run an interactive loop to test strings of text directly against the models.

**For BERT:**
```bash
python src/models/test_bert.py
```
**For Classical ML (Logistic Regression):**
```bash
python src/models/predict.py
```

### Method C: Batch Evaluation Pipeline
To evaluate the models on the full test dataset and generate professional Markdown reports (like the ones in the `docs/` folder):

```bash
python src/models/evaluate_bert.py --evaluate --eval_samples 1000
```
*(Setting `--eval_samples 0` will evaluate the entire dataset).*
